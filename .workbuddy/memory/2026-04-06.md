# 2026-04-06 工作日志

## GitHub 友链仓库搭建

完成了一套完整的 GitHub 友链仓库模板，包含：

- **links.json** — 友链数据文件（meta + links 数组结构）
- **index.html** — 纯静态前端展示页，特性：亮/暗主题切换、标签筛选、实时搜索、骨架屏加载、响应式卡片网格
- **README.md** — 详细的友链提交教程（Issue 方式 + PR 方式）
- **CONTRIBUTING.md** — PR 规范、自检清单、JSON 验证方法
- **.github/ISSUE_TEMPLATE/friend-link.md** — GitHub Issue 模板
- **LICENSE** — MIT 许可证

数据字段：id / name / url / avatar / description / tags / color / added
部署方式：GitHub Pages（免配置）/ Vercel / Netlify
本地预览端口：8765 (python -m http.server 8765)

## UI 重设计（同日）

- 全黑暗系设计（深色 #08090d 底色 + 亮色模式切换）
- Aurora 光晕动画背景（3个模糊光球 drift 动画）
- 噪点纹理遮罩提升质感
- 顶部 Nav 含 GitHub 链接 + 主题切换
- Hero 渐变标题 + 徽章 + 统计卡片
- 卡片：顶部 accent 线、头像光晕环、箭头淡入、玻璃渐变遮罩
- 去除了 links.json 中的 added 字段

## 第三次迭代（同日）

- 项目名改为「友链交换站」，副标题改为「以下为收录站点，如需加入可在 GitHub 提交 PR」
- links.json 结构：meta + categories 数组 + links 数组
- 固定5个分类（不可自定义）：life生活博客 / tech技术博客 / design设计创意 / photo摄影记录 / open开源项目
- 点击卡片弹出详情 Modal（banner渐变+头像+描述+分类标签+复制链接+访问站点）
- 统计数值（收录站点/分类标签/上次更新）完全从 data 动态计算，不写死

## 第四次迭代（同日）—— Modal 头像 + bg 字段 + 头像降级

- 修复头像被裁切：modal-avatar 移到 banner 内部居中（absolute left:50% transform:-50%），banner 高度改为 130px
- 增加 bg 字段（可选）：支持纯色、CSS 渐变、背景图三种形式，覆盖默认 color 渐变
- 头像降级策略：avatar 为空时自动尝试 favicon.ico；favicon 失败显示 🌐 emoji
- modal-url 加 🔗 图标装饰，modal-body 改为居中排版
- README 补充「没有头像怎么办」表格（favicon / GitHub头像 / Gravatar / dicebear / 留空）
- README 补充 bg 字段用法示例
- links.json 第一条示例加 bg 字段演示
- Modal 新增头像链接行：头像缩略图 + 链接文本 + 复制按钮，方便别人获取头像 URL
- bg 字段 url() 语法修复：正确设置 background-image + background-size:cover + background-position:center
- modal-copy-avatar 按钮样式独立
- Banner 默认改为优先显示站点背景图（og-image.jpg），bg 字段可覆盖；不再默认显示纯色渐变
